# NOTES — 为什么这样搭,以及我没解决什么

## 1. 为什么用"模拟回答器"而不是真 LLM

真实系统里,一次错误回答可能来自三个完全不同的原因:检索没召回、重排排错、模型自己编。
三者混在一起时,**你无法归因,也就无法改进**。

所以我先把回答器换成行为可控的模拟器,让"证据在不在、排第几"成为唯一变量。
这不是为了省 API 费用,而是为了**让每个环节的贡献可分离**。

代价也很清楚:结论说明的是"检索 + 拒答"这条链,不能直接说"某个 LLM 会怎样"。
我在 README 里把这条限制写在最显眼的位置,而不是塞进脚注。

## 2. 为什么一定要画"风险—覆盖率"曲线,而不是报一个阈值

我一开始的想法是"找一个最好的阈值"。做完才发现这个问法是错的:

* τ=0.30 时精确率最高(0.755),但它**牺牲了 18% 的覆盖率**;
* τ>0.55 之后系统什么都不答——**精确率无从谈起**;
* 换句话说:**"最好的阈值"不存在,只有"最合适的代价权衡"**。

于是输出的形式改成了一张表 + 一个 AURC 汇总值:**让曲线说话,让决策留给业务**。
拒答率本身就是成本,它必须被显式计入,而不是被藏在"精确率提升"的叙事里。

## 3. 曲线非单调这件事,我不想掩盖

τ=0.40 时精确率掉到 0.550,低于 τ=0.30 的 0.755。可能的原因:

1. **重排置信度不是校准过的概率**。逻辑回归在类别不平衡(正例 22.7%)下训练,
   输出概率的绝对值没有校准,拿它当"不确定性"用是本仓库最大的方法论弱点;
2. **样本量太小**。测试集 60 条,τ=0.40 处只有 40 条被回答,单个样本的翻转就能改变几个百分点。

正确的下一步是做**可靠性图(ECE)**——先证明这个分数配得上"置信度"这个名字,再用它拒答。
我没有做,因为那需要更大的测试集(至少几百条),而这个骨架目前只用合成数据。

## 4. 尚未解决 / 存疑

- **没有与真实 LLM 对照**:`answer_sim` 的三条概率(0.90 / 0.55 / 0.20)是我拍的,没有依据;
  换真模型是第一优先。
- **检索只用 BM25**:生产系统里是"向量 + 关键词 + 重排"的混合召回,这里少了向量那一路;
- **没有多轮与工具调用**:真实 Agent 会追问、会调工具,而本仓库只处理单轮问答;
- **阈值扫描用的是同一批测试数据**:严格来说应该在验证集上选 τ、在测试集上报结果,
  当前写法会让 τ 的估计偏乐观;
- **AURC 只在 19 个离散阈值上算**,与文献里的连续定义不完全可比。

## 5. 这条线和我另外两个仓库的关系

三个仓库看起来在三件事上,其实是一个问题的三个切面:

| 仓库 | 系统在问自己什么 |
|---|---|
| `uq-state-estimation` | 我对当前状态的估计**有多可信**?(传感器退化时) |
| `table-structure-gnn` | 我复原出来的结构**站不站得住**?(一条错边会毁掉整列) |
| `rag-eval-abstention` | 我给出的答案**有没有依据**,没依据时**该不该拒答**? |

**三个都在同一个问题上:系统如何知道自己什么时候不可信,并据此降级、保守化或拒答。**
这也是我在个人主页研究兴趣里写的那条主线——不是三个题目,是一个问题的三次实例化。
